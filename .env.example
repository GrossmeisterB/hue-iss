# REQUIRED — generate via `python -c 'import secrets; print(secrets.token_hex(32))'`
FLASK_SECRET=

# Optional — overrides
TIMEZONE=Europe/Zurich
IP_GEO_URL=https://ipapi.co/json/
DATA_DIR=/data
PORT=5057
